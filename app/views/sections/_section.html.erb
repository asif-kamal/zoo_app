<%= form_with model: section do |section_form| %>
    Name: <%= section_form.collection_select(:user_id, Section.all, :id, :name) %>
    Environment: <%= section_form.text_field :environment %>
    <% section_form.hidden_field :user_id %>

      <%= section_form.submit %>
      <% end %>


